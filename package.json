{
  "name": "euler-prob-59",
  "version": "1.0.0",
  "description": "",
  "main": "build/main.js",
  "scripts": {
    "genVals": "tsc && cd build && node genValues.js && cd ..",
    "testVals": "tsc && cd build && node testValues.js && cd .."
  },
  "author": "Oscar Ratcliffe",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "@types/node": "^20.11.28"
  }
}
